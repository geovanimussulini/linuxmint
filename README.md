# linuxmint
integração do Git e Github no Linux Mind 22.2 Zara
Geovani Mussulini

1  poweroff
    2  Bash
    3  curl -fsS https://dl.brave.com/install.sh | sh
    4  sudo pacman -Sy brave-browser
    5  sudo apt install curl
    6  sudo curl -fsSLo /usr/share/keyrings/brave-browser-archive-keyring.gpg https://brave-browser-apt-release.s3.brave.com/brave-browser-archive-keyring.gpg
    7  sudo curl -fsSLo /etc/apt/sources.list.d/brave-browser-release.sources https://brave-browser-apt-release.s3.brave.com/brave-browser.sources
    8  sudo apt update
    9  sudo apt install brave-browser
   10  sudo apt install curl
   11  sudo curl -fsSLo /usr/share/keyrings/brave-browser-archive-keyring.gpg https://brave-browser-apt-release.s3.brave.com/brave-browser-archive-keyring.gpg
   12  sudo curl -fsSLo /etc/apt/sources.list.d/brave-browser-release.sources https://brave-browser-apt-release.s3.brave.com/brave-browser.sources
   13  sudo apt update
   14  sudo apt install brave-browser
   15  curl
   16  sudo apt install curl
   17  sudo gpg --dearmor -o /usr/share/keyrings/brave-browser-archive-keyring.gpg
   18  sudo apt update
   19  sudo apt install brave-browser
   20  sudo apt install apt-transport-https curl
   21  sudo curl -fsSLo /usr/share/keyrings/brave-browser-archive-keyring.gpg https://brave-browser-apt-release.s3.brave.com/brave-browser-archive-keyring.gpg
   22  echo "deb [signed-by=/usr/share/keyrings/brave-browser-archive-keyring.gpg arch=amd64] https://brave-browser-apt-release.s3.brave.com/ stable main" | sudo tee /etc/apt/sources.list.d/brave-browser-release.list
   23  sudo apt update
   24  sudo apt install brave-browser
   25  sudo apt update && sudo apt install chromium-browser
   26  s
   27  chromium-browser
   28  sudo apt install chromium-browser
   29  sudo apt update && sudo apt install chromium-browser
   30  sudo init 0
   31  sudo apt update
   32  sudo -i
   33  sudo apt update
   34  sudo apt upgrade
   35  sudo apt update
   36  sudo apt upgrade
   37  sudo apt dist-upgrade
   38  sudo apt upgrade -y
   39  -a
   40  --configure -a
   41  sudo apt autoremove
   42  sudo apt update
   43  sudo apt install git vim pip python3
   44  sudo dpkg --configure -a
   45  git clone https://github.com/geovanimussulini/linuxmint
   46  git pull
   47  git add
   48  git pull
   49  history
